name: flutter_riverpod_crash_course
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  
  flutter:
    sdk: flutter
  custom_lint: ^0.7.6
  flutter_dotenv: ^6.0.0
  flutter_riverpod: ^2.4.13
  google_fonts: ^6.3.1
  riverpod_annotation: ^2.6.1
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.13
  flutter_lints: ^6.0.0
  riverpod_lint: ^2.6.5
  riverpod_generator: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - .env
    - assets/products/
